<!-- <script lang="ts">
  // We're setting the theme as css class on the body tag, therefore we do not need the store here anymore.
  import { currentTheme } from '../../stores/liquidStore'
</script> -->

<ld-bg-cells class={`block absolute inset-0 min-h-full`} />